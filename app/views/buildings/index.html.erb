<h1>物件一覧</h1>
<%= notice %>
<table>
 <thead>
    <tr>
      <th>物件名</th>
      <th>賃料</th>
      <th>住所</th>
      <th>築年数<th>
      <th>備考</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  <thead>
  <tbody>
  <% @buildings.each do |building| %>
    <tr>
      <td><%= building.building_name %></td>
      <td><%= building.rent %></td>
      <td><%= building.address %></td>
      <td><%= building.age_of_building %></td>
      <td><%= building.remark %></td>
      <td><%= link_to '詳細',building_path(building.id)%></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= link_to '物件を登録する', new_building_path %>